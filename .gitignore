# Never publish dependency installs or OS cruft
node_modules/
package.json
package-lock.json
.DS_Store

# Regenerated by the sync script but kept only under archive/ (unused by the site)
data/xlsx/board_archive_pointers.xlsx
